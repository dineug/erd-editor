<!-- Parent: ../../AGENTS.md -->
<!-- Generated: 2026-08-27 | Updated: 2026-09-26 -->

# vscode-extension

## Purpose

The published VSCode extension (`vuerd-vscode`, publisher `dineug`) — the Node-side host. It registers the `editor.erd` custom editor for `*.erd`, `*.erd.json`, `*.vuerd` and `*.vuerd.json`, owns the document bytes, serves the bundle `vscode-webview` builds into `public/`, and answers the `webview-bridge` commands. `private: true`; published through `vsce`.

## Key Files

| File | Description |
| --- | --- |
| `package.json` | Also the extension manifest: `contributes` (custom editor, four `vuerd.*` commands, three `dineug.erd-editor.theme.*` settings, `dineug.erd-editor.agentHub.enabled`), `activationEvents`, `engines` |
| `src/extension.ts` | `activate`: `DocumentRegistry.makeUnsafe(platform)`, the provider, the four `vuerd.*` commands and one `ManagedRuntime` of `extensionLive(registryLive(registry), documentHubLive(version))`; `deactivate` awaits that runtime's dispose under a 5 second timeout, which VSCode waits for and a subscription's `dispose` does not get, then closes the registry |
| `src/hub/index.ts` | The three layers `activate` composes: `registryLive` (the registry over `agent-hub-host`'s native realpath), `documentHubLive` (`agent-hub-host`'s `documentHubLayer` given this window's handlers, `vscodeHost`, the registry as its documents and `nodeHubServices(version)`) and `extensionLive`, `agent-hub-host`'s `withDocumentHub`, which builds the registry first and the hub on it, and logs a hub that fails to build instead of failing the runtime |
| `src/hub/` | The document side of the hub coding agents join: the registry, the handlers and the host adapter, on effect layers. The connection side — the per-window pipe advertised by `~/.erd-editor/ide/<pid>.json`, serving `hello` and every later request, path authorization, the lock file, the node layers and the hub's lifecycle — is `@dineug/erd-editor-agent-hub-host`'s; its `AGENTS.md` holds those rules |
| `src/hub/vscodeHost.ts` | `vscodeHost`, this window as the hub's `HubHost`: `ide` `vscode`, `isHubEnabled`, the file-scheme workspace folders, trust and the setting as the enabled event, folder changes as the folder event; `registryDocuments`, the registry as its `HubDocuments` |
| `src/hub/documentRegistry.ts` | `DocumentRegistry` — open documents keyed by real path, resolved on the effect `FileSystem` its `DocumentRegistry.layer` is given, those registered before that layer built one at a time in arrival order, their webviews (it owns `docToWebviewMap`), the ready set, joined peers, `observedVersion`, the active document; `deliverToPeer` / `injectToWebviews` are the only ways out |
| `src/hub/handlers.ts` | `createDocumentHandler(registry, fs)` — the `HubHandler`: `listDocuments`, `openDocument`, `join`, `applyActions`, `leave`, `save` — and its `HubHandlerService` layer; `agent-hub-host`'s `ERD_FILE_EXTENSIONS` (the custom editor selector, unit-asserted) builds `ERD_FILE_GLOB`, the activation glob, and `assertErdFile` refuses any other path with `badRequest`; every refusal the Obsidian plugin answers alike comes from `agent-hub-host`'s `documentRules.ts` constructors, only the read-only view ones are this host's own |
| `src/hub/readonlyUri.ts` | `isReadonlyUri` — the one readonly rule, used by `Editor.readonly` and the hub |
| `src/erd-editor-provider.ts`, `src/erd-document.ts` | The `CustomEditorProvider` (registers documents and panels with the `DocumentRegistry`, tracks the active panel) and the `CustomDocument` owning the bytes |
| `src/editor.ts` | Abstract `Editor` — the `Bridge`, `buildHtmlForWebview` (`{{extension-base-url}}`), `readonly` through `isReadonlyUri` |
| `src/erd-editor.ts` | `ErdEditor` — every host-side command: initial value, save, replication broadcast, import/export dialogs, theme push |
| `vite.config.ts` | CJS lib build to `dist/extension.js` (`ssr`, `ssr.noExternal: true`, `target: 'node22'`, `minify: true`, `sourcemap: false`, only `vscode` and builtins external) plus `run.tasks`. `publicDir: false`, or Vite copies `public/` into `dist/` and the VSIX ships the webview twice |
| `tsconfig.unit.json` | The type gate over `src`, specs and the stub. `exclude: []` is load-bearing: inheriting the parent's typechecks zero specs and still exits 0 |

## Subdirectories

| Directory | Purpose |
| --- | --- |
| `test/mocks/` | The `vscode` stub the unit suite aliases in; `hubLayers.ts`, the memory `FileSystem`, `HubEnvironment` and `HubListener` layers, the memory `Socket` pair the hub specs run on, `agent-hub-host`'s production layers over them with this window's adapter (`startMemoryHub`), and the registry over them — built (`createMemoryRegistry`) or not yet (`createPendingRegistry`); `documentHarness.ts`, a registry + handler + provider building the real `ErdEditor` |
| `test/integration/` | Mocha specs for a live Extension Host over `test/fixtures/workspace/`; `erd-json-only/` holds the specs of the run over `test/fixtures/erd-json-only/` |
| `public/`, `dist/`, `out/` | Generated: the webview bundle (never edit), the extension bundle, the compiled integration specs |

## For AI Agents

### Working In This Directory

- `package.json` is the manifest. Its src ↔ manifest invariants are unit-asserted: `src/constants/viewType.test.ts`, the defaults check in `src/configuration.test.ts`, the commands and menu entries in `src/extension.test.ts`, the `agentHub.enabled` key and default in `src/hub/config.test.ts`, and in `src/hub/autoOpen.test.ts` `ERD_FILE_EXTENSIONS` against `customEditors[].selector` and `ERD_FILE_GLOB` against `activationEvents`.
- `engines.vscode` (`^1.101.0`, the first release running on Node 22) is the floor. `.vscode-test.mjs` derives its `minimum-supported` run from it; `build.target: 'node22'`, `@types/node` `^22` and `@types/vscode` `^1.101` are raised by hand with it, and nothing gates them.
- **`dist/extension.js` is a contract**: `main` is `./dist/extension`, so a `.cjs` emit never activates. Everything but `vscode` and builtins inlines because the VSIX ships no `node_modules` (`--no-dependencies`); a bare `require` left in the bundle fails activation.
- **Minified, and the VSIX carries no map.** Vite writes no map unless asked and leaves an `ssr` build unminified, so `build.minify: true` is what strips this bundle and `build.sourcemap` is now `false`; `rolldownOptions.output.minify` on top emits the same bytes (sha256 equal, measured 2026-09-23), so only the one option is set. The reason is Effect. Measured 2026-09-23 when that was decided, while `@dineug/erd-editor-agent-hub` was the only thing pulling effect in: the bundle had gone 72,473 B → 424,302 B unminified, minification brought it to 116,599 B (38,494 B by the `gzip -9` command line, 38,495 B at the `zlib` level 9 used below), and a map of that would have been 2,082,178 B; the VSIX went 1,851,779 → 1,378,150 B, 18 files → 17. The hub has run on effect since, so the Build bullet under Testing Requirements holds today's size. `.vscodeignore` holds `dist/**/*.map` so that stays true structurally: the build task's `tsc` step snapshots `dist/**` before `vp build` empties it, so an entry recorded while maps were on restores one beside the minified bundle on every later cache hit, and `vsce` packs whatever sits in `dist/` (measured 2026-09-23: 17 files with such a map present). A stack trace from the Extension Host therefore names `dist/extension.js` and a column on one of its few lines: reproduce with such a local build rather than reading the shipped trace.
- File support lives in two manifest places — `activationEvents` (`workspaceContains:**/*.{erd,vuerd,erd.json,vuerd.json}`) and `customEditors[].selector` — and their extension sets must stay equal: the hub's lock exists only once the extension is active. A glob of `**/*.{erd,vuerd}` alone leaves a folder holding only `.erd.json` / `.vuerd.json` files with the extension inactive and no lock; the `erd-json-only` Extension Host run asserts such a folder activates it.
- The hub serves only while `isHubEnabled()` (`src/hub/config.ts`): the window is trusted and `dineug.erd-editor.agentHub.enabled` (default `true`) is not off. It re-applies on `onDidGrantWorkspaceTrust` and on a change to that setting, the enabled event of `vscodeHost`; off or untrusted, it still writes a `hub: false` lock. Its lock covers the file-scheme workspace folders only, since a virtual folder guards no path on disk.
- **The connection side is `agent-hub-host`'s** (`packages/agent-hub-host/AGENTS.md`): serving a connection and its frames, the reader and writer halves, `fromNetSocket`, path authorization with its `lstat` climb, the native realpath every path the hub takes goes through, the lock file and stale sweep, the publisher and its `PUBLISH_WAIT`, and the one-runtime rule of `withDocumentHub`. This package supplies the document side through `HubHandlerService`, `HubHost` (`vscodeHost`) and `HubDocuments` (the registry) and never calls `releaseSync`: `deactivate` awaits the runtime's dispose, which runs the hub's `close`.
- The hub never throws at the editor: every failure goes to `console.warn` under `[erd-editor hub]`. `activate` makes the `DocumentRegistry` itself, with no IO, and registers the provider and the commands synchronously, so a document can open the moment it returns; then it starts one `ManagedRuntime` of `extensionLive`. Its registry layer builds first, cannot fail, and hands the registry its IO; the hub's layer builds on it and, when it fails, is logged as `could not start the document hub` while the registry serves every editor (`extension.test.ts` pins it). The registry logs a publish that rejects, and `deactivate` catches every cause of the runtime's disposal and then closes the registry, which settles whatever it still queues when the dispose interrupted its layer mid-build.
- **Registration is a queue the registry's layer drains.** `register` and `unregister` change what the editor side reads (`docToWebviewMap`, the entries) at once and queue only their IO step, the native realpath of a new document, run on the `FileSystem` of `DocumentRegistry.layer` through a `FiberSet` of its scope. Steps queued before that layer built wait for it and then run one at a time in arrival order, each after the IO of the one before it and never after its publish, so the lock takes that backlog in the order it came; a step that comes later runs at once, on its own, so a realpath that hangs on a stalled mount holds up only its own document, whose editor does not open until it returns and so takes no edit its lock entry would guard. Each step is followed by a publish, and the lock lists only paths a step resolved. Closing the layer interrupts the steps in flight and settles the rest without IO. A realpath that fails or dies keys the document by the path given, the key a path that is not absolute, an untitled document's, always gets. The platform, which decides how keys compare from the first call on, goes to `makeUnsafe`; only the realpath is IO.
- `openCustomDocument` awaits `register`, which never rejects and awaits the publisher the hub hands the registry: that resolves at once while the hub is coming up, and once the hub is up waits for the lock rewrite that lists the document for one second at most (`PUBLISH_WAIT`, `agent-hub-host`'s `src/services/DocumentHub.ts`), so an agent resolving the file finds this window's lock before the editor takes an edit and a stalled task ahead of that rewrite holds no editor opening (`extension.test.ts` pins both). A stalled task or a listen that never returns also holds the hub's close, which `deactivate` abandons at its 5 second timeout, leaving the lock for the stale sweep of a window that starts after this process is gone.
- **One way to a peer, one way into webviews.** Everything bound for a peer goes through `DocumentRegistry.deliverToPeer` (join queue, then the two filters: version at or below `snapshotVersion`, or no version at all); everything an agent sends webviews goes through `injectToWebviews` (ready webviews only, no exclusion). A webview that has not sent `hostInitialCommand` would wipe an injected batch with its initial value. Only batches queued before the snapshot is captured go through the filters; one queued between the capture and the timer that ends the window is not in the snapshot and goes out whole.
- A peer's batch is `applyActions`, refused unless the document has a ready webview and the peer joined that very `ErdDocument`; a webview never relays it back (it arrives tagged shared), so `injectToPeers` is the only way the other peers hear of it. `join` (500 ms cap) and `save` (2 s cap) first wait for the replica saves a change is owed, since `document.content` lags an edit by the replica's 200 ms debounce; the quiet state and its rules are `agent-hub-host`'s `joinWindow.ts`, with the webview as the view. The webviews owing one are the ready set copied when the change is noted, and the wait ends once that set is empty and at least one save has come: a webview that reports ready afterwards never holds the wait open and its own save settles nothing by itself, while a change that reached no ready webview still waits for one save. `onValueSaved` therefore carries the webview whose replica saved, and a panel closing takes it out of the set. A save reaching the hub sooner than 200 ms after a peer batch was sent cannot hold it and is not counted; a relay bounds nothing, since it can arrive after its replica saved. `join` captures at its cap regardless; `save` answers `saved: false` at its cap and writes nothing, as the bytes may lack the edit.
- `readonly` is the two schemes of `src/hub/readonlyUri.ts`, `git` and `conflictResolution`; `vscode.workspace.fs.isWritableFileSystem` was considered and not used. A git or merge view shares its file's path: the registry prefers the file, and with only the view open `join` says `readonly: true` and writes are refused, but `openDocument` neither answers from the view nor waits on it — it opens the file itself.
- **The registry's keys are the native realpath's**, as every other path the hub takes: `registryLive` provides the registry layer with `agent-hub-host`'s `nativeFileSystemLayer`, so a folder VS Code hands over in NFC whose name the disk holds in NFD, or a differently cased path on macOS, is keyed as the disk spells it and as the MCP server resolves it. `src/hub/nodeRegistry.test.ts` holds both spellings to the disk's.
- Unit specs must not provide the node layers (`agent-hub-host`'s `hubEnvironmentLayer`, `hubListenerLayer`, `nativeFileSystemLayer` and `nodeHubServices`, `NodeFileSystem.layer`, or `registryLive` and `documentHubLive`, which carry them) — build the memory layers of `test/mocks/hubLayers.ts` instead, or they bind a socket and write a lock under the real home directory. The one exception is `src/hub/nodeRegistry.test.ts`, which runs the registry over `registryLive` against a `mkdtemp` directory and builds no hub.
- A panel's `onDidDispose` listener must not read `panel.webview`: VS Code marks the panel disposed before firing the event and the getter throws `Webview is disposed` (seen on 1.138.0, and on 1.90.0 when it was the floor; history now), which once left a closed split-view panel counted ready, so `save` waited on a replica that no longer existed. `removeWebview` finds the webview by its panel, and the stub's panel throws the same way. It then calls `dropRecipient` (`agent-hub-host`'s `joinWindow.ts`) for that webview: the closed replica's save never comes, so a pending change whose remaining replicas already saved settles right then, not at the next edit.
- State-changing commands must go through `dispatchBroadcast` over `docToWebviewMap` in `src/erd-editor.ts`, or one document open in two groups desynchronizes. The one exception is the hub: an agent's batch goes through `DocumentRegistry.injectToWebviews` to every ready webview, since no webview sent it, while `dispatchBroadcast` skips the webview a command came from.
- New host behaviour goes on `ErdEditor`, not the provider, which only wires documents and panels into the registry; `bootstrapWebview` returns one `Disposable` releasing both the `mergeRegister` handle and the raw listeners.
- No new `vuerd.*` command: `extension.test.ts` and the Extension Host suite pin the registered set to the manifest's four.
- The four `vuerd.*` handlers are arrows, never bare references: `editor/title` passes more than the uri, and the second argument would land as `viewColumn`.
- `IMPORT_FILE_TYPES` is keyed by the bridge's import `type` union and feeds both the dialog filter and the extension check; the check is an alternation because `graphql` has three extensions.
- `capabilities.untrustedWorkspaces.supported` is `true` — never eval or execute document content.
- `build:vsce` / `publish:vsce` run a `vsce` no package.json declares; it must be on `PATH`.

### Testing Requirements

- Unit: `pnpm exec vp run --filter vuerd-vscode --fail-if-no-match test`. Call `resetVscodeMock()` in `beforeEach`; the stub is shared per file. `pnpm --filter vuerd-vscode typecheck` is the gate alone.
- Integration: `pnpm --filter vuerd-vscode e2e` builds, compiles `tsconfig.integration.json` to `out/`, then runs `stable` and `minimum-supported` over `test/fixtures/workspace/` and `erd-json-only` (stable only) over a folder whose one file is `sample.erd.json`, with its own specs (`xvfb-run -a` on Linux; `VSCODE_TEST_USER_DATA_DIR` shortens the IPC socket path). Those specs import only `vscode` and node builtins, the fake peers of `agent-hub.test.ts` included — the host runs the emitted JS unbundled, so `@/` does not resolve; `agent-hub.test.ts` speaks the hub's JSON lines by hand over `net` and restores the `sample.erd.json` it saves.
- `vscode.workspace.save(uri)` saves a custom editor document and clears its dirty flag: measured 2026-09-22 on VS Code 1.138.0, two tabs of one document went from dirty to clean within 1 ms of one call and the disk held the new table; a later probe got the same from that call alone on 1.90.0 (8 ms) and 1.138.0 (8 ms), so the hub's save ladder ends at its first rung on stable, and did on 1.90.0 (measured on 1.90.0 when it was the floor; history now).
- `agent-hub.test.ts` prints one `[agent-hub e2e] vscode <version>` line per timed spec (join, applyActions, applyToDirty, save, applyToOtherPeer); applyToDirty is the replica round trip that `join` and `save` wait on, about 210–225 ms on 1.90.0 and 1.138.0 (measured on 1.90.0 when it was the floor; history now), against their 500 ms and 2 s caps.
- The replica round trip runs in a real Extension Host: measured 2026-09-22, each of two webviews answered an injected batch with `hostSaveValueCommand` 209–239 ms later, `document.content` grew 900 → 1936 B, the tab turned dirty at 256 ms, and the `Tag.shared` batch never came back through `hostSaveReplicationCommand`.
- Build: `pnpm exec vp run --filter vuerd-vscode --fail-if-no-match build`; `dependsOn` builds `vscode-webview` first, which fills `public/`. Nothing gates the bundle's size, so it is recorded here: `dist/extension.js` is 167,870 B (gzip-9 55,784 B, the `zlib.gzipSync` level the size scripts use, measured 2026-09-26) with the connection side in `agent-hub-host` behind the `HubHost` and `HubDocuments` services, `releaseSync` and `drain` (166,640 B and 55,386 B before, measured 2026-09-23 with the hub on effect layers, the registry's IO on a layer of its own, an editor opening's wait on the lock capped, effect imported from its named entries, peer frames decoded by `PeerToHubMessage` and notifications encoded by `HubNotification`; 166,474 B and 55,332 B before the cap, 164,212 B and 54,507 B before the registry's layer, 163,664 B and 54,300 B before the notification encoder, 163,668 B and 54,496 B with effect's module paths), up from 116,599 B (gzip-9 38,495 B) when only `agent-hub` pulled effect in. Two minified spikes set what to expect of it, measured 2026-09-23: a minimal `Layer.effect` + `ManagedRuntime` + one `Schema.Struct` extension came to 129,231 B, and one shaped like this hub (Stream, Queue, Deferred, a Socket adapter, Ndjson, a tagged error, a Logger) to 154,361 B, so the bundle sits above the shape it ships and inside 1.5× either one. It carries no `ws`, no `undici` and no dynamic `import(`, and requires only `vscode` and node builtins.
- Manual: F5 `Launch Extension` from this folder builds the dependencies once (`build:deps`), then watches the extension only — rebuild the webview after changing it. The watch build is the shipping one, so it is minified and mapless too: `launch.json`'s `outFiles` still finds `dist/extension.js`, but a breakpoint belongs in it and not in `src/`. Flip `build.sourcemap` to `true` for a debugging session, and do not commit that.

## Dependencies

### Internal

`@dineug/erd-editor-webview-bridge`, `@dineug/erd-editor-agent-hub` and `@dineug/erd-editor-agent-hub-host` (all inlined into `dist/`, one copy of `agent-hub` for this package and `agent-hub-host`, which keeps it a bare import) and `@dineug/erd-editor-vscode-webview` (fills `public/`) — `devDependencies`, which the build's `dependsOn` still follows.

### External

`effect` (core modules from `effect`; `Socket` from `effect/unstable/socket` in `test/mocks/` only) and `@effect/platform-node`, `devDependencies` from the root catalog's exact pin and inlined into `dist/extension.js`: the same copies `@dineug/erd-editor-agent-hub` and `@dineug/erd-editor-agent-hub-host` name as their peers. Nothing here imports `@effect/platform-node` any more; it is listed for `agent-hub-host`, whose `NodeFileSystem` (by module path, since the barrel needs `redis`, which nothing here installs) the bundle inlines. Effect is imported from the entries its `package.json` exports by name, never a module path, which `src/hub/imports.test.ts` holds for all of `src/` and `test/`. `@vscode/test-cli` and `@vscode/test-electron` for the Extension Host suite.

<!-- MANUAL: notes added below this line are preserved on regeneration -->
