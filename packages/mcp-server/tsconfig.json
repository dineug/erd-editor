{
  "extends": "../../tsconfig.app.json",
  "compilerOptions": {
    // A Node program: no DOM globals, so a browser API cannot slip in unseen.
    "lib": ["ES2022"],
    "types": ["node"],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
