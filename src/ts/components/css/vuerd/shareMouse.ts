import { css } from "lit-element";

export const shareMouse = css`
  /* =============== shareMouse ============== */
  .vuerd-share-mouse {
    position: absolute;
    z-index: 100000000;
    color: var(--vuerd-theme-font-active, var(--vuerd-color-font-active));
  }
`;
