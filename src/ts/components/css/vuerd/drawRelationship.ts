import { css } from "lit-element";

export const drawRelationship = css`
  /* =============== drawRelationship ============== */
  .vuerd-draw-relationship {
    position: absolute;
    top: 0;
    stroke: var(--vuerd-theme-key-fk, var(--vuerd-color-key-fk));
  }
`;
